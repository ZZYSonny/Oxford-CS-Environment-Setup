{
	"version": "1.0.0",
	"configurations": [
		{
			"cwd": "${fileDirname}",
			"executable": "foo.elf",
			"name": "PyOCD(foo)",
			"request": "launch",
			"type": "cortex-debug",
			"servertype": "pyocd",
			"postStartSessionCommands": [
				"monitor reset",
				"continue"
			],
			"preLaunchTask": "Build"
		},
		{
			"cwd": "${fileDirname}",
			"executable": "${fileBasenameNoExtension}.elf",
			"name": "PyOCD(Cur)",
			"request": "launch",
			"type": "cortex-debug",
			"servertype": "pyocd",
			"postStartSessionCommands": [
				"monitor reset",
				"continue"
			],
			"preLaunchTask": "Build"
		}
	]
}